# Place your Cloudflare Origin CA files here (never committed):
#   origin.pem  — the certificate
#   origin.key  — the private key
# Dashboard: SSL/TLS -> Origin Server -> Create Certificate (RSA, 15 years,
# hostnames: your domain + *.yourdomain).
