export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-rule">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-5 py-8 text-sm text-muted sm:flex-row sm:items-baseline sm:justify-between sm:px-8">
        <p>Cream paper by day, ink by night. Click through to the source.</p>
        <p>Parchment · a link library</p>
      </div>
    </footer>
  );
}
